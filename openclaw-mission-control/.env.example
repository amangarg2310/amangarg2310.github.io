# OpenClaw Mission Control — Environment Variables
#
# Copy this file to .env.local and fill in values.

# ──────────────────────────────────────────────────
# API URL (optional)
# ──────────────────────────────────────────────────
# When set, the frontend fetches from this URL instead of the
# built-in Next.js API routes. Use this to point at an external
# OpenClaw runtime or standalone backend.
#
# Leave unset for demo mode (uses built-in mock data via /api routes).
#
# NEXT_PUBLIC_API_URL=http://localhost:4000/api

# ──────────────────────────────────────────────────
# OpenClaw Runtime Connection
# ──────────────────────────────────────────────────
# URL of the OpenClaw runtime to pull live agent/run/task data from.
# When set, the dashboard polls this URL every SYNC_INTERVAL_MS and
# normalizes runtime data into the dashboard schema.
#
# When NOT set, the dashboard runs in demo mode with mock data.
#
# Expected runtime endpoints:
#   GET /agents   → raw agent list
#   GET /sessions → raw session/run list (with events + messages)
#   GET /tasks    → raw task list
#
# OPENCLAW_RUNTIME_URL=http://localhost:5555

# ──────────────────────────────────────────────────
# Sync Interval (optional)
# ──────────────────────────────────────────────────
# How often (in ms) to poll the runtime. Default: 15000 (15s).
# Minimum: 5000 (5s).
#
# SYNC_INTERVAL_MS=15000

# ──────────────────────────────────────────────────
# Auth (optional — for future use)
# ──────────────────────────────────────────────────
# API_SECRET=your-secret-here
